#include <bits/stdc++.h>
using namespace std;
#define fastio ios::sync_with_stdio(false); cin.tie(NULL);
int maximum(int a, int b){
    return (a>=b ? a : b);
}
 
int main(){
    fastio
    int t,n,inp;
    cin >> t;
    //vector<int> a,b,x;
    while(t--){
        //a.clear();
        cin >> n;
        cout << (n/2)+1 << endl;
        
    }
    return 0;
}

 
 


